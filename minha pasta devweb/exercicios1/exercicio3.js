// Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.

function baseElevadaExpoente (base, expoente) {
    resultado = base ** expoente
    return resultado
}

console.log(baseElevadaExpoente(4, 3))